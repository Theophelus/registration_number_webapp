module.exports = function (pool) {
    //define a function call setReg with one parameter
    let setReg = async (regNumbers) => {
        //create regEx for numbers
        RegNumbers = /^[a-zA-Z]{2,3}(\s)[0-9]{3}(\-)[0-9]{3}$/;

    }

    return {
        
    }
}