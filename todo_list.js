// 1) define a function call setRegistrations that that take in one parameter call numbers
        // - check if registration number fall within our scope.
        // - then if that condition is true, if true add that registration
        // - else false display a message registreation already exists